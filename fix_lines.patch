--- src/sandbox/linux/net_relay.rs
+++ src/sandbox/linux/net_relay.rs
@@ -266,3 +266,4 @@
 
-    let allowed = if let Ok(tty) = std::fs::OpenOptions::new().read(true).write(true).open("/dev/tty") {
+    let allowed = if let Ok(tty) =
+        std::fs::OpenOptions::new().read(true).write(true).open("/dev/tty")
+    {
@@ -275,3 +276,6 @@
         let mut buf_reader = std::io::BufReader::new(timeout_reader);
-        let mut writer = std::fs::OpenOptions::new().write(true).open("/dev/tty").unwrap_or_else(|_| std::fs::File::create("/dev/null").unwrap());
+        let mut writer = std::fs::OpenOptions::new()
+            .write(true)
+            .open("/dev/tty")
+            .unwrap_or_else(|_| std::fs::File::create("/dev/null").unwrap());
         prompt_confirmation_interactive(
