# codex-rescue-darwin-arm64

Platform binary package for the Codex Rescue Alpha5 prerelease. CI injects the PyInstaller-built macOS arm64 executable before `npm pack`. The repository does not contain a prebuilt binary.
