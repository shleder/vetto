# codex-rescue-darwin-x64

Platform binary package for the Codex Rescue Alpha5 prerelease. CI injects the PyInstaller-built macOS x64 executable before `npm pack`. The repository does not contain a prebuilt binary.
