Working in /home/shleder/prod/vetto (branch feat/phase1-contract-authority). With recon results from A1/A2/A3 and master-task section 6 (verbatim, below), implement process containment battery using PROC-ESC-001 as canonical canary where it already covers the class. Cover parent->child->grandchild, fork, spawn, daemonize, setsid, detach, survive-parent-exit, session/group escape, continue-after-termination. Known documented Linux FS-ONLY detached-grandchild gap: do NOT declare PASS; emit FAIL/INCONCLUSIVE with exact reason per existing verdict contract. Do not fix Phase 3 scope here. Independent runtime evidence only. Write code + tests; no local builds/tests; no commit/push. Report changed files with rationale and NOT PROVEN items.

--- MASTER TASK SECTION 6 ---
## 6. PROCESS CONTAINMENT
parent → child → grandchild; fork; spawn; daemonize; setsid; detach; survive parent exit; escape process group/session; continue after supervised execution terminates. PROC-ESC-001 — canonical canary. Linux FS-ONLY: documented detached-grandchild gap — НЕ объявлять PASS; FAIL/INCONCLUSIVE с точной причиной по существующему verdict contract. Не исправлять Phase 3 внутри Phase 2.

--- RECON SUMMARY & CONTEXT ---
- Existing checks:
  * PROC-ESC-001 (src/verify_ng/registry.rs:227, Proc blocker canary)
  * HANG-GRANDCHILD-001 (src/verify_ng/registry.rs:179)
  * CLEANUP-SIGKILL-001 (src/verify_ng/registry.rs:269)
  * setsid_daemon_escape (src/redteam.rs:85)
  * test_linux_proc_escape_001, test_linux_grandchild_001, test_linux_tree_kill_001, test_linux_orphan_001 in tests/integration/verify_ng_linux_enforce.rs
  * test_process_tree_extinction_theorem_cases in tests/phase4_enterprise_runtime.rs
- Subreaper sweep by session nonce exists in src/sandbox/production.rs:1020 and src/verify_ng/linux_enforce.rs:147.
- Important: On Linux FS-ONLY (where PID namespaces are not active), detached grandchildren can escape session tree. Master task section 6 explicitly mandates: do NOT declare PASS for Linux FS-ONLY detached grandchild; emit FAIL/INCONCLUSIVE with exact reason per existing verdict contract.
- Independent runtime evidence only (HOST_FACT / subreaper scan / proc tree inspection).

UNIVERSAL CONSTRAINTS:
- Repo access: /home/shleder/prod/vetto only. Branch feat/phase1-contract-authority.
- No local cargo build/test/check/run; GitHub CI is the only execution proof.
- No sandbox escape payload development or bypass techniques; defensive verification batteries only.
- No new architecture, no second policy engine, no Phase 3/4 scope, no version bump, no release actions.
- No commit, no push, no secrets access, no external network calls beyond your model service.
- Separate confirmed facts (file:line) from assumptions in every report.
