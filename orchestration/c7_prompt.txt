Working in /home/shleder/prod/vetto (branch feat/phase1-contract-authority). With recon results from A1/A2/A3 and master-task section 14 (verbatim, below), verify CLI, MCP and multi-agent entrypoints all run verification through the same sealed-contract production boundary. If any branch bypasses sealed contract authority, fix it within existing architecture (no new security layer, no second policy engine, no verifier overrides). Add regressions proving parity across entrypoints. Write code + tests; no local builds/tests; no commit/push. Report changed files with rationale and NOT PROVEN items.

--- MASTER TASK SECTION 14 ---
## 14. CLI / MCP / MULTI
Verification через production boundary независимо от entrypoint: CLI; MCP; multi-agent. Ветка, обходящая sealed contract authority, исправляется в Phase 2. Отдельные security implementations не создавать.

--- RECON SUMMARY & DEFECTS FLAGGED BY RECON A3 ---
1. CLI `--verify` (preflight) in src/main.rs:815-834:
   - Called `vetto::verify::preflight` which historically called `doctor::probe::run_probe_script` bypassing `SecurityContract` and calling `backend.spawn` directly.
   - Note C1 added `preflight_contract(&SecurityContract)` to `src/verify.rs`. Ensure preflight routes through sealed contract authority (`SecurityContract` -> `UnpreparedProductionExecution` / `preflight_contract`).
2. CLI `vetto verify` command in src/main.rs:333-341:
   - Must use sealed-contract authority path.
3. MCP in src/mcp/mod.rs:226-277:
   - `execute_sandboxed_command` ran `sh -c` / `cmd.exe /C` and parsed stderr for "BLOCKED" / "denied".
   - MCP `wrap` (src/mcp/wrap.rs:421) already uses `UnpreparedProductionExecution`. Unify MCP so no shell strings are used, arguments are passed directly, and sealed contract boundary is authoritative.
4. Multi-agent (src/multi/runtime.rs:339):
   - Already uses `UnpreparedProductionExecution::new` -> `prepare()?.spawn()?`.
5. Parity regressions:
   - Add integration tests demonstrating that all three entrypoints (CLI, MCP, multi-agent) run through the same sealed-contract production boundary and enforce identical contract rules (e.g. invalid contract rejected across all entrypoints).

UNIVERSAL CONSTRAINTS:
- Repo access: /home/shleder/prod/vetto only. Branch feat/phase1-contract-authority.
- No local cargo build/test/check/run; GitHub CI is the only execution proof.
- No sandbox escape payload development or bypass techniques; defensive verification batteries only.
- No new architecture, no second policy engine, no Phase 3/4 scope, no version bump, no release actions.
- No commit, no push, no secrets access, no external network calls beyond your model service.
- Separate confirmed facts (file:line) from assumptions in every report.
