Working in /home/shleder/prod/vetto (branch feat/phase1-contract-authority). With recon results from A1/A2/A3 and master-task section 5 (verbatim, below), implement environment isolation battery consuming the SAME sealed SecurityContract: arbitrary host variable, sensitive-looking variable, PATH manipulation, inherited environment, internal Vetto variables, explicitly denied variables, post-start mutation. Verifier must distinguish 'allowed by contract' from 'present because host leaked'; no blanket prefix assumptions unless contract semantics define them. Extend ENV-LEAK-001 rather than duplicating. Independent runtime evidence only. Write code + tests; no local builds/tests; no commit/push. Report changed files with rationale and NOT PROVEN items.

--- MASTER TASK SECTION 5 ---
## 5. ENVIRONMENT ISOLATION
Запрещённые host environment values не должны попадать в execution. Минимум: arbitrary host variable; sensitive-looking variable; PATH manipulation; inherited environment; internal Vetto variables; variables explicitly denied by contract; environment mutation после старта. Verifier различает "allowed by contract" и "present because host environment leaked". Никаких blanket assumptions ("starts with X => safe") вне contract semantics.

--- RECON SUMMARY & CONTEXT ---
- Existing checks:
  * ENV-LEAK-001 (src/verify_ng/registry.rs:243, Secrets blocker canary)
  * ENV-POISON-001 (src/verify_ng/registry.rs:154, Spawn blocker)
  * trap_env_poison_fails_blockers (tests/integration/verify_ng_traps.rs:160)
  * adv_proxy_secrets_stripped_but_neighbors_kept, adv_proxy_beats_explicit_passthrough, adv_proxy_env_extra_merge_must_be_restripped in tests/integration/adv_isolation.rs
- Note C1 updates:
  * ExecutionRequest has `pub contract: Option<&'a SecurityContract>`
  * tests/integration/verify_ng_boundary_contract.rs was added in tests/integration/
  * Runner pass_capable now supports blocker categories when boundary evidence is present
- Verifier must distinguish 'allowed by contract' from 'present because host leaked'. Do not use blanket prefix assumptions.
- Independent runtime evidence only (HOST_FACT / environment capture via supervisor, not unverified child self-report).

UNIVERSAL CONSTRAINTS:
- Repo access: /home/shleder/prod/vetto only. Branch feat/phase1-contract-authority.
- No local cargo build/test/check/run; GitHub CI is the only execution proof.
- No sandbox escape payload development or bypass techniques; defensive verification batteries only.
- No new architecture, no second policy engine, no Phase 3/4 scope, no version bump, no release actions.
- No commit, no push, no secrets access, no external network calls beyond your model service.
- Separate confirmed facts (file:line) from assumptions in every report.
