[metadata]
name = "devin"
description = "Compatibility profile for Devin CLI environment."

[filesystem]
allow_read = ["$AGENT"]
allow_write = ["$AGENT"]

[environment]
pass_through = [
    "DEVIN_*",
]

[display_only_deny]
paths = [
    "$AGENT/*.sock",
]
