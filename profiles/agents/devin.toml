[metadata]
name = "devin"
description = "Compatibility profile for Devin CLI environment."

[filesystem]
allow_read = [
    "$AGENT",
    "/tmp",
    "$HOME/.cache",
]
allow_write = [
    "$AGENT",
    "/tmp",
    "$HOME/.cache",
]

[environment]
pass_through = [
    "DEVIN_*",
]
