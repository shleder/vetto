[metadata]
name = "copilot"
description = "Safe read-only compatibility roots for GitHub Copilot CLI."

[filesystem]
allow_read = ["$AGENT/cache", "$AGENT/logs"]

[display_only_deny]
paths = ["$AGENT/hosts.json", "$AGENT/config.json"]
