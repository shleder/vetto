[metadata]
name = "swe_agent"
description = "Compatibility profile for SWE-Agent benchmark runner."

[filesystem]
allow_read = ["$AGENT"]
allow_write = ["$AGENT"]

[environment]
pass_through = [
    "SWE_AGENT_*",
    "OPENAI_API_KEY",
    "ANTHROPIC_API_KEY",
]
