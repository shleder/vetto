[metadata]
name = "gpt_engineer"
description = "Compatibility profile for GPT-Engineer CLI."

[filesystem]
allow_read = ["$AGENT"]
allow_write = ["$AGENT"]

[environment]
pass_through = [
    "OPENAI_API_KEY",
]
